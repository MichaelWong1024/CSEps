CC = gcc
CFLAGS = -Wall -Wextra -g
TARGET = connections
SRC = connections.c
OBJ = connections.o

all: $(TARGET)

$(TARGET): $(OBJ)
	$(CC) $(CFLAGS) -o $(TARGET) $(OBJ)

$(OBJ): $(SRC)
	$(CC) $(CFLAGS) -c $(SRC)

clean:
	rm -f $(OBJ) $(TARGET)
